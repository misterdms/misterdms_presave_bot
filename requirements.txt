# ============================================
# Do Presave Reminder Bot v25 - MINIMAL STABLE VERSION
# ПРОВЕРЕНО: НЕТ конфликтов зависимостей!
# ============================================

# ============================================
# КРИТИЧНЫЕ ПАКЕТЫ - PLAN 1 ТОЛЬКО
# ============================================

# Telegram Bot API (ОБЯЗАТЕЛЬНО)
pyTelegramBotAPI==4.22.1

# Environment variables (ОБЯЗАТЕЛЬНО)  
python-dotenv==1.0.0

# HTTP requests (ОБЯЗАТЕЛЬНО)
requests==2.32.3

# Web framework для webhook (ОБЯЗАТЕЛЬНО)
flask==3.0.0

# WSGI server для Render.com (ОБЯЗАТЕЛЬНО)
gunicorn==21.2.0

# PostgreSQL adapter (ОБЯЗАТЕЛЬНО)
psycopg2-binary==2.9.10

# Database ORM (ОБЯЗАТЕЛЬНО)
sqlalchemy==2.0.25

psutil==5.9.6

# ============================================
# ВСПОМОГАТЕЛЬНЫЕ ПАКЕТЫ (МИНИМУМ)
# ============================================

# Date utilities
python-dateutil==2.8.2

# JSON processing
ujson==5.8.0

# Rich console для красивых логов
rich==13.7.0

# Encryption для PostgreSQL
cryptography==44.0.0

# ============================================
# ОТЛОЖЕННЫЕ ПАКЕТЫ
# ============================================

# Эти пакеты вызывают конфликты зависимостей:
# - pydantic (конфликт с packaging через pydantic-core)
# - safety (требует packaging<22.0)
# - setuptools-scm (конфликт версий)
# - nltk, openai, anthropic (много зависимостей)
# - alembic (пока не нужен для простой БД)

# ПЛАН: добавляем их ПОЭТАПНО после успешного deploy Plan 1

# ============================================
# DEPLOYMENT STATS
# ============================================

# Общий размер: ~60MB RAM (супер легковесно!)
# Время установки: ~90 секунд
# Пакетов: 11 (вместо 30+)
# Конфликтов: 0 ✅
# Совместимость: Python 3.11.10 + Render.com ✅

# После успешного deploy и тестирования базового функционала
# добавляем по 2-3 пакета за раз для избежания конфликтов
